apiVersion: aigateway.envoyproxy.io/v1alpha1
kind: MCPRoute
metadata:
  name: terraform-mcp-route
  namespace: terraform
spec:
  parentRefs:
    - name: ai-gateway
      namespace: default
      kind: Gateway
      group: gateway.networking.k8s.io

  path: "/mcp/terraform"

  backendRefs:
    - name: terraform-mcp
      kind: Backend
      group: gateway.envoyproxy.io
      path: "/mcp"

---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: terraform-mcp
  namespace: terraform
spec:
  endpoints:
    - fqdn:
        hostname: terraform-mcp.terraform.svc.cluster.local
        port: 8080
