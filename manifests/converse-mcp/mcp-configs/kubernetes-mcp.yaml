apiVersion: aigateway.envoyproxy.io/v1alpha1
kind: MCPRoute
metadata:
  name: kubernetes-mcp-route
  namespace: converse-mcp
spec:
  parentRefs:
    - name: ai-gateway
      namespace: default
      kind: Gateway
      group: gateway.networking.k8s.io

  path: "/mcp/kubernetes"

  backendRefs:
    - name: kubernetes-mcp
      kind: Backend
      group: gateway.envoyproxy.io
      path: "/mcp"
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: kubernetes-mcp
  namespace: converse-mcp
spec:
  endpoints:
    - fqdn:
        hostname: kubernetes-mcp.converse-mcp.svc.cluster.local
        port: 8080